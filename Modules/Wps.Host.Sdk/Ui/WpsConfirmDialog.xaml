<Window x:Class="Wps.Module.Hosting.Ui.WpsConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        MinWidth="420"
        MaxWidth="640"
        Background="#252526"
        Foreground="#E0E0E0"
        FontFamily="Segoe UI"
        FontSize="13">
    <Window.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="90"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Background" Value="#3A3A3C"/>
            <Setter Property="Foreground" Value="#F0F0F0"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4A4A4C"/>
                    <Setter Property="BorderBrush" Value="#777"/>
                </Trigger>
                <Trigger Property="IsDefault" Value="True">
                    <Setter Property="Background" Value="#0E639C"/>
                    <Setter Property="BorderBrush" Value="#1177BB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Margin="24,20,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Sous-titre / contexte (reason) — affiché si non vide -->
        <TextBlock x:Name="ReasonText"
                   Grid.Row="0"
                   FontSize="12"
                   Foreground="#9090A0"
                   Margin="0,0,0,8"
                   TextWrapping="Wrap"/>

        <!-- Question principale (ask) -->
        <TextBlock x:Name="QuestionText"
                   Grid.Row="1"
                   FontSize="14"
                   TextWrapping="Wrap"
                   LineHeight="22"/>

        <!-- Boutons générés dynamiquement (right-aligned) -->
        <ItemsControl x:Name="ButtonsHost" Grid.Row="3" HorizontalAlignment="Right">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </Grid>
</Window>
